{
  "name": "reactive-fs",
  "version": "0.4.1",
  "description": "Reactive file system interface",
  "license": "MIT",
  "author": {
    "name": "Guillaume Chau"
  },
  "repository": {
    "url": "https://github.com/Akryum/reactive-fs.git",
    "type": "git"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && node esbuild.js && tsc -d --emitDeclarationOnly",
    "watch": "tsc -d -w --sourceMap",
    "test": "peeky run"
  },
  "devDependencies": {
    "@peeky/cli": "^0.4.2",
    "@types/fs-extra": "^9.0.6",
    "@types/node": "^14.14.21",
    "@vue/reactivity": "^3.0.5",
    "chokidar": "^3.4.2",
    "esbuild": "^0.13.12",
    "fs-extra": "^9.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  },
  "files": [
    "/dist"
  ]
}
